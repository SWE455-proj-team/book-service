name: Deploy book-service

on:
  push:
  workflow_dispatch:

jobs:
  deploy:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/service-deploy.yml
    with:
      service_name: book-service
      service_dir: .
